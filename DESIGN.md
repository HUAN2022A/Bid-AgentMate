# Bid-AgentMate — 技术标书智能编写平台 · 产品设计文档

> 版本：v0.2（2026-08-20，经 grilling 13 轮问答定稿）
> 定位：把 bid-skills（AI 编程助手 skill 家族）升级为独立软件产品
> 源码仓库参考：`git@github.com:HUAN2022A/bid-skills.git`（skill 版，已脱敏）

---

## 一、产品定位与核心转变

**一句话**：技术标书（技术文件卷）的半自动编写平台——输入招标文件（docx/pdf），输出逐点响应评分标准、有真实公司素材支撑、排版规范的 `技术文件.docx` 终稿。

**从 skill 到软件的核心转变**：

| | skill 模式 | 软件模式 |
|---|---|---|
| 流程驱动 | AI 助手驱动，人盯流程 | **软件状态机驱动流程**，AI 做内容，人只做决策 |
| 阶段契约 | 文件系统交接（yaml/md 文件） | 数据库 + 显式状态机 |
| 人工确认 | 约定（改 status 字段） | **系统强制**（不确认，起草按钮灰掉） |
| 用户 | 懂技术的个人 | 销售/售前/技术/商务协作团队 |

核心理念不变：**标书 = 逐点响应评分标准 + 真实素材支撑**。评分点对照表仍是贯穿全流程的中枢契约。

## 二、产品边界（Q1 定稿）

**只做"编写"这一段，把"招标文件分析"做透**。完整投标链条（商机登记 → 分析 → 编写 → 评审 → 装订上传 → 开标跟踪 → 归档）里：

- **做**：招标文件分析（bid-parse 的分析报告支撑"要不要投"的决策）+ 技术卷编写
- **不做**：商务/价格卷（责任边界，软件里这些模块直接不存在）、评审/装订/归档（OA/流程系统的事，做了反而重）

## 三、产品形态与部署（Q2/Q3 定稿）

**Web 应用**（不做桌面软件），团队协作场景天然支持多人分权。

**部署形态**：**先私有化单租户，架构上预留多租户能力但不开通**。

- 数据库设计时 tenant_id 字段先留好，但 v1 只有一个租户
- Docker Compose 一键本地部署是底线（投标数据不能出内网）
- SaaS 版等有明确小客户付费意愿再开，避免一开始背两套运维

**LLM 依赖**：**架构上支持 OpenAI 兼容协议，LLM 接入做成配置项**（base_url + api_key + model 名）。

- 客户内网有 GPU：指到本地 vLLM/Ollama（跑 Qwen/DeepSeek 开源模型）
- 客户允许出网：指到云端 API
- 明确告知客户"本地模型能力影响起草质量"，兜底是"AI 生成 + 人工编辑"工作台

**技术栈**：

- 后端：FastAPI（Python，与现有 skill 脚本同语言，extract/check/export 逻辑直接复用）
- 前端：Vue 3 或 React
- 数据库：PostgreSQL（素材卡资格字段用 jsonb 支持可扩展查询）
- 文件存储：MinIO / S3
- 异步任务：Celery / RQ（PDF 解析、批量起草是分钟级任务）
- LLM：OpenAI 兼容协议抽象层

## 四、架构：数据库 + 状态机

### 4.1 项目状态机

```
已上传 → 已解析 → 大纲待确认 → 起草中 → 待自查 → 待导出 → 已完成
              ↑________人工确认点（唯一，系统强制）________↑
```

- **大纲不点"确认"，起草按钮不可用**
- 状态流转全部留痕（谁、什么时候、做了什么），满足审计

### 4.2 大纲变更处理（Q7 定稿）

**允许修改大纲，已起草章节标记"大纲已变更，需复核"**。

- outline_nodes 表加 version 字段，大纲每次确认生成版本号
- chapters 表记 outline_version，大纲变更后 version 不匹配的章节自动标"大纲已变更"
- 自查中心列出需复核章节（内容可能还能用，只是结构归属变了）
- 不完全锁死（招标方澄清文件是常态），也不打回作废（太浪费）

### 4.3 数据模型（核心表）

```
projects          项目（名称、招标编号、状态、创建人、时间、tenant_id 预留）
tender_files      招标文件（原始文件、提取全文、解析结果 JSON）
scoring_items     评分点（category 技术/商务/价格/资质、分值、原文、挂章）
tech_requirements 技术需求（★标记、原文、响应章节）
outline_nodes     大纲章节树（id、标题、目标字数、挂评分点、version）
chapters          章节正文（md 内容、字数、covers 声明、锁定标记、outline_version、版本链）
materials         素材卡（类型 案例/人员/资质/专利、固定核心字段 + qual_extra jsonb、来源文件）
material_snapshots 素材引用快照（项目引用时的卡片内容副本，历史可追溯）
figures           插图（文件、图注、所属章节）
check_reports     自查报告（覆盖率、风险项、待补清单、修复记录）
exports           导出版本（docx 文件、模板、时间、操作人）—— v2
template_mappings 模板章节锚点映射表（可保存复用）
users / roles     用户与角色 —— v1 只分"管理员"和"编辑"
```

### 4.4 协作模型（Q4 定稿）

**串行接力 + 章节级锁定，不做实时协同**。

- 状态流转驱动接力（销售建项目 → 售前分析 → 技术写 → 商务审）
- 章节级"锁定/认领"：这章我在写，别人只读
- 不做 OT/CRDT 实时协同——投标场景 1-2 个技术写，同时改同章需求很弱，实时协同是技术炫技

### 4.5 素材库（Q5/Q9 定稿）

**全局素材库 + 项目引用快照**。

- 素材卡全局唯一（公司业绩就一份），所有项目引用
- 项目引用时存快照（material_snapshots 表），素材卡后续更新不影响已完成标书
- 进行中项目可选"更新到最新版"
- Git 的"引用 commit 而非 branch"思路

**资格字段**：**固定核心字段 + 可扩展 JSON**。

- 固定通用字段：金额、客户、时间、证明材料状态
- 行业特定字段放 qual_extra jsonb 列，管理员界面可"添加自定义字段"（字段名+类型+必填）
- 种子客户（电厂）的"摘复正钩"做成固定字段（高频查询走 SQL），其他行业塞 jsonb

## 五、核心模块（对应原 skill）

| 软件模块 | 对应 skill | 软件化后的关键变化 |
|---|---|---|
| 解析引擎 | bid-parse | 上传即解析（异步任务）；评分点拆解结果**可编辑表格**，人工界面修正 |
| 大纲设计器 | bid-outline | 章节树**拖拽编辑**，评分点挂章"拖评分点到章节"；确认带二次确认 |
| 素材库 | company-knowledge | 独立管理后台：案例/人员/资质卡 CRUD，资格字段结构化可筛选，按资格条件自动筛选合格案例 |
| 起草工作台 | bid-draft | 左右分栏：左章节树+评分点，右 AI 起草实时编辑器；每章"重新生成"+"锁定"按钮 |
| 自查中心 | bid-check | 报告仪表盘：覆盖率矩阵表格、废标风险红卡、待补可勾选任务列表；--fix 变"一键修复"按钮 |
| 导出中心 | bid-export | 模板管理 + 导出预览 + 版本历史（v2） |

## 六、AI 的角色：内容生产者，只在两个环节出现

**流程由状态机驱动，AI 只在解析和起草两个环节出现**：

1. **解析环节**：评分点拆解、废标条款识别调 LLM API（结构化输出 JSON 写库）
2. **起草环节**：每章"生成"按钮触发 LLM 调用，上下文组装（评分原文 + 素材卡 + 历史章节）继承 bid-draft 逻辑

**好处**：可计量（每章记 token）、可替换（OpenAI 兼容协议）、可降级（API 挂了流程不崩）、可审计（输入输出留痕）。

### 6.1 起草的生成粒度（Q8 定稿）

**段落级重写 + 整章重写兜底**。

- 整章重写：v1 兜底（首次生成）
- 段落级重写：日常高频——编辑器选中段落 → "AI 重写" → 选中段落 + 本章评分点 + 上下文发 LLM，只替换选中部分
- 前端记录选区，后端 prompt 明确"只输出重写后的这一段"

### 6.2 自查的 AI 判断（Q11 定稿）

**机器检查和 AI 深度审查分开**。

- 机器检查：快（秒级），"自查"按钮默认跑（覆盖率/废标风险/待补/--fix）
- AI 深度审查：慢且贵（每章一次 LLM 调用），**v1 只做逐章审查**（起草工作台对单章点"AI 审查"），全卷批量审查 v2 再做

## 七、导出与模板（Q10 定稿）

**官方模板章节锚点：自动识别 + 人工确认映射表，映射表可保存复用**。

- 自动识别给草稿（按标题样式匹配，80% 准确率够），人工在界面看到"模板标题 → 大纲章节"映射表，逐条确认或下拉修改
- **关键**：同一招标单位模板反复用，映射表存 template_mappings，下次上传同模板直接加载，人工只改差异
- 把"一次性识别成本"摊薄到"首次配置，后续复用"

## 八、数据红线（软件化后更要守死）

1. **私有化部署必须支持**：Docker Compose 一键本地部署是底线
2. **公司事实只准引用素材库（硬约束软件化）**：起草时 AI 上下文只注入素材库内容；生成正文出现素材库没有的公司名/业绩数字时，自查模块自动标红
3. **联网查技术资料做成可开关功能**，查询日志留痕
4. **商务/价格卷模块在软件里直接不存在**——防止"顺手做了"导致责任不清

## 九、商业模式

- **按项目收费**比订阅合理——投标是脉冲式需求
- **私有化部署 + 年费**是央企/国企客户唯一能接受的模式
- **素材库是护城河**——用得越久迁移成本越高

**种子客户策略（Q6 定稿）**：先贴着现在服务的公司做种子客户，v1 功能 = 这家公司真实投标流程的最小闭环。投标软件需求极其场景化，没有真实客户打磨通用产品必死；种子客户用顺了再抽象通用能力。v1 数据模型可贴着"翻车机机器人"这类项目做，不追求行业通用。

## 十、v1 范围（Q12/Q13 定稿）

### v1 必须有（最小闭环）

- 上传招标文件 → 自动解析（评分点/技术需求/废标条款）
- 大纲编辑 + 确认（状态机卡死）+ 大纲变更标记"需复核"
- 素材库管理（案例/人员卡录入查询，固定字段 + jsonb 扩展）
- 逐章 AI 起草（整章生成 + 段落级重写）+ 人工编辑 + 章节锁定
- 机器自查（覆盖率/废标风险/待补/--fix）
- 逐章 AI 深度审查
- 章节级复用（**手动指定历史项目复用**，ROADMAP C 方案）
- 版本历史（**章节编辑器层面**，chapters 版本链）
- 导出 docx（**通用排版兜底**）

### v1 砍掉，v2 再做

- ~~官方模板自动识别~~（v1 用通用排版，能过大多数电子标）
- ~~多用户分权~~（v1 只分"管理员"和"编辑"两种角色）
- ~~全卷批量 AI 审查~~（v1 只逐章）
- ~~章节复用自动推荐~~（v1 只手动指定，ROADMAP A 方案 v2）
- ~~导出 docx 版本历史~~（v1 只章节编辑器版本）

**v1 核心验证目标**：种子客户能用它写完一份真标书并中标。任何不直接影响这个目标的功能都砍。

## 十一、实施路径

### 阶段 1：脚本层服务化（1 周出 demo）

把 `extract_pdf.py`、`check_bid.py`、`export_docx.py` 包成 FastAPI 接口，前端最简上传/下载界面。验证最短闭环：**上传招标文件 → 自动解析 → 下载分析报告**。

### 阶段 2：LLM 编排层（核心工作量）

把 SKILL.md 里"模型判断"的环节变成稳定 API 调用：

- 评分点拆解：LLM + 结构化输出（JSON schema 约束）+ 失败重试
- 上下文组装：从文字描述变成代码（评分原文 + 素材卡 + 历史章节拼装）
- 逐章起草：prompt 工程 + 输出校验（长度、必含要素、禁含报价）

难点在 LLM 输出不确定性处理——结构化输出约束 + 校验 + 人工兜底编辑界面。

### 阶段 3：协作与管理

多用户、角色权限、素材库后台、版本历史、审计日志。

## 十二、原 skill 版已验证的能力基线

以下能力在 skill 版（bid-skills）已全流程验证，软件化时直接继承：

- 评分办法拆解（总分制 + 分卷加权制两种模式），与人工分析 10 项完全一致
- PDF 直读（pdfplumber，92 页真实招标 PDF 131 个表格全还原）
- 24 章 5.4 万字起草，5/5 技术评分项全覆盖
- 6 种 matplotlib 图型自动配图，docx 嵌入导出
- 自查 --fix 四项低风险自动修复
- 素材库入库（140 业绩 → 11 案例卡，资格字段结构化）

---

## 附：设计决策记录（grilling 13 轮问答）

| # | 决策 | 结论 |
|---|---|---|
| Q1 | 产品边界 | 只做"编写"，把"招标文件分析"做透；商务/价格卷不做，评审/装订/归档不做 |
| Q2 | 部署形态 | 先私有化单租户，架构预留多租户（tenant_id）但不开通 |
| Q3 | LLM 依赖 | OpenAI 兼容协议 + 配置项（base_url/api_key/model），支持本地 vLLM/Ollama |
| Q4 | 协作粒度 | 串行接力 + 章节级锁定，不做实时协同 |
| Q5 | 素材库 | 全局素材库 + 项目引用快照 |
| Q6 | 商业验证 | 贴着种子客户做，v1 = 真实投标流程最小闭环 |
| Q7 | 大纲变更 | 允许修改，已起草章节标"大纲已变更，需复核"（version 字段） |
| Q8 | 起草粒度 | 段落级重写 + 整章重写兜底 |
| Q9 | 资格字段 | 固定核心字段 + 可扩展 jsonb |
| Q10 | 模板锚点 | 自动识别 + 人工确认映射表，映射表可保存复用 |
| Q11 | AI 审查 | 机器检查和 AI 深度审查分开，v1 只逐章审查 |
| Q12 | v1 范围 | 砍官方模板识别、多用户分权，其他保留 |
| Q13 | 保留项边界 | AI 审查只逐章、复用只手动指定、版本历史只章节编辑器层面 |

## 附：与原 skill 仓库的关系

- 本软件是 bid-skills 的**产品化演进**，不是替代——skill 版继续适合个人/技术用户快速用
- 核心算法（解析、检查、导出脚本）从 skill 仓库移植，保持同步
- 数据红线、评分点契约、人工确认点等设计理念完全继承
