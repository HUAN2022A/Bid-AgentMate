# Bid-AgentMate — 技术标书智能编写平台 · 产品设计文档

> 版本：v0.1（2026-08-20）
> 定位：把 bid-skills（AI 编程助手 skill 家族）升级为独立软件产品
> 源码仓库参考：`git@github.com:HUAN2022A/bid-skills.git`（skill 版，已脱敏）

---

## 一、产品定位与核心转变

**一句话**：技术标书（技术文件卷）的半自动编写平台——输入招标文件（docx/pdf），输出逐点响应评分标准、有真实公司素材支撑、排版规范的 `技术文件.docx` 终稿。

**从 skill 到软件的核心转变**：

| | skill 模式 | 软件模式 |
|---|---|---|
| 流程驱动 | AI 助手驱动，人盯流程 | **软件状态机驱动流程**，AI 做内容，人只做决策 |
| 阶段契约 | 文件系统交接（yaml/md 文件） | 数据库 + 显式状态机 |
| 人工确认 | 约定（改 status 字段） | **系统强制**（不确认，起草按钮灰掉） |
| 用户 | 懂技术的个人 | 销售/售前/技术/商务协作团队 |

核心理念不变：**标书 = 逐点响应评分标准 + 真实素材支撑**。评分点对照表仍是贯穿全流程的中枢契约。

## 二、产品形态

**Web 应用**（不做桌面软件）。理由：

- 标书是团队协作场景（销售拿标、售前分析、技术写方案、商务报价），Web 天然支持多人分权
- AI 能力走 API，桌面软件无本地优势
- 私有化部署需求（见数据红线）用 Web 也能满足（Docker 交付）

**技术栈建议**：

- 后端：FastAPI（Python，与现有 skill 脚本同语言，extract/check/export 逻辑直接复用）
- 前端：Vue 3 或 React
- 数据库：PostgreSQL
- 文件存储：MinIO / S3（招标文件、章节 md、导出 docx、图片）
- 异步任务：Celery / RQ（PDF 解析、批量起草是分钟级任务，不能卡界面）
- LLM：可配置的 API 接入（OpenAI 兼容协议，支持切换国产模型/私有部署模型）

## 三、架构：文件系统契约 → 数据库 + 状态机

### 3.1 项目状态机

```
已上传 → 已解析 → 大纲待确认 → 起草中 → 待自查 → 待导出 → 已完成
              ↑________人工确认点（唯一，系统强制）________↑
```

- 每个状态对应原 skill 的一个阶段，触发方式从"人输 /bid-parse"变成"界面按钮/上传后自动流转"
- **大纲不点"确认"，起草按钮不可用**——比 skill 里"拒绝开写"的提示硬得多
- 状态流转全部留痕（谁、什么时候、做了什么操作），满足审计要求

### 3.2 数据模型（核心表）

```
projects          项目（名称、招标编号、状态、创建人、时间）
tender_files      招标文件（原始文件、提取全文、解析结果 JSON）
scoring_items     评分点（category 技术/商务/价格/资质、分值、原文、挂章）
tech_requirements 技术需求（★标记、原文、响应章节）
outline_nodes     大纲章节树（id、标题、目标字数、挂评分点、状态）
chapters          章节正文（md 内容、字数、covers 声明、锁定标记、版本）
materials         素材卡（类型 案例/人员/资质/专利、结构化字段、来源文件）
figures           插图（文件、图注、所属章节）
check_reports     自查报告（覆盖率、风险项、待补清单、修复记录）
exports           导出版本（docx 文件、模板、时间、操作人）
users / roles     用户与角色（销售/售前/技术/商务/管理员）
```

### 3.3 与原 skill 的对应关系

| 软件模块 | 对应 skill | 软件化后的关键变化 |
|---|---|---|
| 解析引擎 | bid-parse | 上传即解析（异步任务）；评分点拆解结果做成**可编辑表格**，人工在界面修正（不用改 YAML） |
| 大纲设计器 | bid-outline | 章节树**拖拽编辑**，评分点挂章做成"拖评分点到章节"；确认按钮带二次确认 |
| 素材库 | company-knowledge | 独立管理后台：案例/人员/资质卡 CRUD，资格字段（金额/正钩/证明状态）结构化可筛选，支持按资格条件自动筛选合格案例 |
| 起草工作台 | bid-draft | 左右分栏：左章节树+评分点，右 AI 起草实时编辑器；**每章"重新生成"和"锁定"按钮**——锁定的章 AI 不再碰，防止重新生成冲掉人工改好的内容 |
| 自查中心 | bid-check | 报告做成仪表盘：覆盖率矩阵表格、废标风险红卡、待补可勾选任务列表；--fix 变"一键修复"按钮，修复留痕 |
| 导出中心 | bid-export | 模板管理（上传官方模板自动识别章节锚点）+ 导出预览 + 版本历史（每次导出存版本可回滚） |

## 四、AI 的角色：从驱动者降级为内容生产者

这是与 skill 模式最本质的区别。**流程由状态机驱动，AI 只在两个环节出现**：

1. **解析环节**：提取全文后，评分点拆解、废标条款识别等需要理解的工作调 LLM API（结构化输出 JSON，写入 scoring_items / tech_requirements 表）
2. **起草环节**：每章"生成"按钮触发一次 LLM 调用，上下文组装逻辑（评分原文 + 素材卡 + 历史章节）就是 bid-draft 的那套，原样搬过来

**这样设计的好处**：

- **可计量**：每章生成记 token 消耗，成本可控
- **可替换**：换模型不改流程（OpenAI 兼容协议抽象层）
- **可降级**：API 挂了流程不崩，只是生成按钮不可用，人工仍可编辑
- **可审计**：每次 AI 调用的输入（上下文）和输出（生成内容）留痕

## 五、数据红线（软件化后更要守死）

1. **私有化部署必须支持**：投标数据（素材库、项目文件）是客户核心资产，很多场景不能出内网——不能用纯 SaaS 模式，Docker Compose 一键本地部署是底线
2. **公司事实只准引用素材库（硬约束软件化）**：起草时 AI 上下文只注入素材库内容；生成正文出现素材库没有的公司名/业绩数字时，自查模块自动标红（"不编造"规则的代码实现）
3. **联网查技术资料做成可开关功能**，查询日志留痕（审计要求）
4. **商务/价格卷不做**：软件里这些模块直接不存在，防止"顺手做了"导致责任不清

## 六、商业模式（如做成产品）

- **按项目收费**比订阅合理——投标是脉冲式需求
- **私有化部署 + 年费**是央企/国企客户唯一能接受的模式（数据不出内网 + 有人维护）
- **素材库是护城河**——客户用得越久，库里案例/人员/模板越多，迁移成本越高

## 七、实施路径（从现有 skill 演进，不推倒重来）

### 阶段 1：脚本层服务化（1 周出 demo）

把 `extract_pdf.py`、`check_bid.py`、`export_docx.py` 包成 FastAPI 接口，前端做最简上传/下载界面。验证最短闭环：**上传招标文件 → 自动解析 → 下载分析报告**。

- 核心脚本现成，工作量在接口封装和异步任务
- 产出：能跑的 Web demo，证明"机器能干的"已软件化

### 阶段 2：LLM 编排层（核心工作量）

把 SKILL.md 里"模型判断"的环节变成稳定的 API 调用：

- 评分点拆解：LLM + 结构化输出（JSON schema 约束）+ 失败重试
- 上下文组装：从 SKILL.md 的文字描述变成代码（评分原文 + 素材卡 + 历史章节的拼装逻辑）
- 逐章起草：prompt 工程 + 输出校验（长度、必含要素、禁含报价）

难点在 LLM 输出的不确定性处理——需要结构化输出约束 + 校验 + 人工兜底编辑界面。

### 阶段 3：协作与管理

多用户、角色权限、素材库后台、版本历史、审计日志。

## 八、原 skill 版已验证的能力基线

以下能力在 skill 版（bid-skills）已全流程验证，软件化时直接继承：

- 评分办法拆解（总分制 + 分卷加权制两种模式），与人工分析 10 项完全一致
- PDF 直读（pdfplumber，92 页真实招标 PDF 131 个表格全还原）
- 24 章 5.4 万字起草，5/5 技术评分项全覆盖
- 6 种 matplotlib 图型自动配图，docx 嵌入导出
- 自查 --fix 四项低风险自动修复
- 素材库入库（140 业绩 → 11 案例卡，资格字段结构化）

---

## 附：与原 skill 仓库的关系

- 本软件是 bid-skills 的**产品化演进**，不是替代——skill 版继续适合个人/技术用户快速用
- 核心算法（解析、检查、导出脚本）从 skill 仓库移植，保持同步
- 数据红线、评分点契约、人工确认点等设计理念完全继承
